<%- model_class = Post -%>
<div class="page-header">
  <h1><%= "Blog" %></h1>
</div>

<%= render @posts %>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_post_path, :class => 'btn btn-primary' if can? :create, @post %>
